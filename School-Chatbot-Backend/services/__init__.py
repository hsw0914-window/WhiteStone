"""Service layer for backend route handlers.

The modules in this package are the boundary between FastAPI routers and the
legacy runtime module. Business logic can move here gradually without changing
router signatures.
"""
